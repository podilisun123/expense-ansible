- name: configure db server
  hosts: db
  become: yes
  tasks:
  - name: install mysql server
    ansible.builtin.dnf:
      name: mysql-server
      state: installed
      
  - name: start mysql server
    ansible.builtin.service:
      name: mysqld
      state: restarted
      enables: yes